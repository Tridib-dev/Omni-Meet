// components/auth/Logo.tsx
//
// ============================================================================
// PLACEHOLDER LOGO — replace whenever you're ready.
// Swap the text below for an <Image src="/logo.svg" ... /> or your own
// logo component. Nothing else in the auth pages needs to change since
// they all just render <Logo /> at the top of the form.
// ============================================================================

export default function Logo() {
  return (
    <div className="flex items-center justify-center lg:justify-start mb-8">
      {/* TODO: replace this placeholder text/box with your real logo */}
      <span className="text-2xl font-bold tracking-tight text-white">
        Omni<span className="text-zinc-400">Meet</span>
      </span>
    </div>
  );
}
